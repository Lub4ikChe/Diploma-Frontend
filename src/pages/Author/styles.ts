import styled from '@emotion/styled';
import { Grid } from '@mui/material';

export const StyledGridAuthorPageWrapper = styled(Grid)`
  background: #161d2a;
  border: 1px solid #212a3b;
  border-radius: 4px;
`;

export const StyledGridHeaderWrapper = styled(Grid)`
  border-bottom: 1px solid #212a3b;
  background: #16243a;
`;
